# NPM configuration
# Override deprecated npm config variables to suppress warnings
# Suppress unknown env config warnings by setting them to valid values
npm-globalconfig=
verify-deps-before-run=false
_jsr-registry=
# Suppress warnings by setting error log level
loglevel=error
fund=false
audit=false