core-contracts/ 
│   └── src/ 
│   ├── auth/ # 🔹 Auth Microservices (TypeScript + Effect + FP) │ ├─
auth-core/ # 🔹 Сущности и базовые типы │ │ 
│   ├── AuthUser.ts # Данные пользователя (id, имя, email,
роли) │ │ 
│   ├── AuthSession.ts # Сессия пользователя (начало, конец, ttl) │ │ 
│   ├── AuthToken.ts # JWT /
access token / refresh token │ │ 
│   ├── Credentials.ts # Логин/пароль / альтернативные креды │ │ ├─
AuthenticationMethod.ts # Тип метода аутентификации (пароль, OAuth, MFA) │ │ 
│   └── index.ts # Экспорт
core сущностей │ │ 
│   ├── auth-context/ # 🔹 Контексты авторизации │ │ 
│   ├── AuthContext.ts # Определение
AuthContext (текущий пользователь, сессия, токен) │ │ 
│   ├── AuthContextRunner.ts # Исполнение кода в
контексте AuthContext (runInContext) │ │ 
│   ├── AuthCorrelation.ts # Корреляция запросов и действий
пользователя │ │ 
│   ├── AuthorizationContext.ts # Контекст прав и ролей пользователя │ │ 
│   └── index.ts #
Экспорт всех контекстов │ │ 
│   ├── auth-services/ # 🔹 Сервисы авторизации │ │ 
│   ├── AuthService.ts #
Основные операции аутентификации (login, logout, refresh) │ │ 
│   ├── AuthorizationService.ts # Проверка
прав и ролей пользователя │ │ 
│   └── index.ts # Экспорт всех сервисов │ │ 
│   ├── auth-errors/ # 🔹 Ошибки и
исключения │ │ 
│   ├── AuthenticationError.ts # Ошибки аутентификации (неверный пароль, пользователь не
найден) │ │ 
│   ├── AuthorizationError.ts # Ошибки прав доступа (permission denied) │ │ ├─
SessionError.ts # Ошибки сессии (expired, invalid) │ │ 
│   ├── TokenError.ts # Ошибки токена (invalid,
expired, malformed) │ │ 
│   ├── AuthError.ts # Базовая ошибка Auth │ │ 
│   ├── AuthErrorCodes.ts # Коды ошибок
Auth │ │ 
│   ├── AuthErrorMapper.ts # Маппинг ошибок в читаемые сообщения │ │ 
│   ├── legacy.ts # Устаревшие
ошибки (для миграции) │ │ 
│   └── index.ts # Экспорт всех ошибок │ │ 
│   ├── auth-specifications/ # 🔹
Спецификации и правила доступа │ │ 
│   ├── PermissionSpecification.ts # Проверка конкретного разрешения │
│ 
│   ├── RoleSpecification.ts # Проверка роли пользователя │ │ 
│   ├── RoleHierarchySpecification.ts #
Проверка иерархии ролей │ │ 
│   └── index.ts # Экспорт всех спецификаций │ │ 
│   ├── auth-validation/ # 🔹
Валидация данных и схемы │ │ 
│   ├── AuthValidationResult.ts # Результаты валидации (успех / ошибки) │ │

│   ├── helpers/ # Вспомогательные функции │ │ │ 
│   ├── validateAndCreate.ts # Валидация данных и создание
объектов │ │ │ 
│   └── index.ts │ │ 
│   └── schemas/ # Схемы для валидации │ │ 
│   ├── credentials-schema.ts #
Валидация credentials │ │ 
│   ├── session-schema.ts # Валидация session │ │ 
│   ├── token-schema.ts #
Валидация токенов │ │ 
│   ├── user-schema.ts # Валидация данных пользователя │ │ 
│   └── index.ts # Экспорт
всех схем │ │ 
│   ├── auth-metadata/ # 🔹 Метаданные (для логов, метрик и контекстов) │ │ ├─
AuthMetricTags.ts # Теги метрик для auth │ │ 
│   ├── AuthMetrics.ts # Определение метрик │ │ ├─
AuthMetricsCollector.ts # Сбор метрик │ │ 
│   └── index.ts # Экспорт метаданных и метрик
