core-contracts/ 
│   └── src/ 
│   ├── ci/ # 🔹 CI микросервисы (TypeScript + Effect + FP) │ 
│   ├── context/ # 🔹
Контексты CI (используются для Propagation и Tenant/RunContext) │ │ 
│   ├── index.ts # Экспорт всех
контекстов CI │ │ 
│   ├── CIContext.ts # Основной CI Context (хранение состояния сборки/деплоя) │ │ ├─
CIContextRunner.ts # Runner для выполнения операций с CI Context │ │ 
│   └── CICorrelation.ts #
Связь/корреляция между CI задачами │ 
│   ├── errors/ # 🔹 ADT ошибки CI микросервисов │ │ 
│   ├── index.ts #
Экспорт всех ошибок CI │ │ 
│   ├── CIError.ts # Базовая ошибка CI │ │ 
│   ├── CIErrorCodes.ts # Коды ошибок CI
│ │ 
│   ├── CIErrorMapper.ts # Маппинг ошибок на коды/сообщения │ │ 
│   └── CIErrorSeverity.ts # Классификация
ошибок по severity │ 
│   ├── metadata/ # 🔹 Типы/интерфейсы CI │ │ 
│   ├── index.ts # Экспорт всех типов CI │
│ 
│   ├── CIBuildInfo.ts # Метаданные сборки │ │ 
│   ├── CIDeployInfo.ts # Метаданные деплоя │ │ ├─
CIEnvParser.ts # Парсер CI окружения │ │ 
│   └── CIProvider.ts # Провайдер CI сервисов (GitHub Actions,
GitLab CI, Jenkins) │ 
│   ├── metrics/ # 🔹 Метрики / Observability для CI │ │ 
│   ├── index.ts # Экспорт всех
метрик │ │ 
│   ├── CIMetrics.ts # Основные метрики CI │ │ 
│   ├── CIMetricsCollector.ts # Сборщик метрик │ │

│   └── CIMetricTags.ts # Теги для метрик CI (pipeline, branch, stage) │ 
│   └── validation/ # 🔹 Schema
Validation для CI payload / конфигураций │ 
│   ├── index.ts # Экспорт всех схем и валидаторов │ ├─
CIValidationResult.ts # Результат валидации │ 
│   └── schemas/ │ 
│   ├── build-schema.ts # Схема сборки │ ├─
deploy-schema.ts # Схема деплоя │ 
│   └── index.ts # Экспорт всех схем
