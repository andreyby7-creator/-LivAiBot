# 🌐 LivAi Platform - Технический стек (2026)

## Backend

### 1. Ядро:

Стек: FastAPI 0.128+ + Pydantic v2.12+ + Uvicorn 0.40+ + uvloop 0.20+
Комментарии: Асинхронность, строгая типизация, быстрые API, production-ready, масштабирование через Gunicorn workers, поддержка WebSocket и HTTP/2. Uvloop для 2-4x прироста производительности asyncio, Hypercorn для нативной HTTP/2 поддержки.

### 2. Безопасность API:

Стек: CORS middleware + slowapi 0.5+ + security headers + Yandex Cloud API Gateway (альтернатива: nginx reverse proxy)
Комментарии: Защита от DDoS, соблюдение Закона РБ "О защите персональных данных" №99-З (аналог 152-ФЗ РФ), контроль доступа, мониторинг API.

### 3. Аутентификация/Авторизация:

Стек: fastapi-users 14.0+ + python-jose[cryptography] 3.4+ + passlib[bcrypt] + Yandex Cloud IAM (альтернатива: Keycloak self-hosted)
Комментарии: Готовые решения для пользователей и enterprise-клиентов, интеграция с облачными сервисами.

### 4. Данные:

Стек: PostgreSQL 17+ + Yandex Managed PostgreSQL (с репликацией) (альтернатива: self-hosted PostgreSQL + репликация), SQLAlchemy 2.0+ + Alembic 1.14+, Redis 8.4+ + Yandex Managed Redis (альтернатива: self-hosted Redis), ClickHouse 25.x (аналитика)
Комментарии: Высокая доступность, масштабируемость, локализация данных, кэширование и очереди через Redis. PostgreSQL репликация для разделения read/write нагрузки, кластеризация для fault tolerance при высоких нагрузках.

### 5. Фоновые задачи:

Стек: Celery 6.0+ + Redis + Yandex Cloud Message Queue (альтернатива: RabbitMQ self-hosted)
Комментарии: Асинхронная обработка, интеграции с внешними сервисами, высокая надёжность.

### 6. AI:

Стек: Yandex Cloud AI SDK 2.0+ + LangChain 1.2+ + YandexGPT Pro
Комментарии: RAG, LLM, мультиагенты, обработка документов, сайтов и баз знаний.

### 7. Векторное хранилище:

Стек: Qdrant 1.16+ (managed) + Chroma 1.4+ (локально) + Pinecone/Weaviate (fallback)
Комментарии: Высокая скорость поиска по embeddings, локальная разработка и прототипирование. Pinecone/Weaviate как fallback для дополнительных LLM сценариев при росте до 100k+ пользователей.

### 7.1 Генерация документов:

Стек: python-docx + reportlab + Jinja2 templates + Pandoc
Комментарии: Создание коммерческих предложений, договоров, отчетов в PDF/DOCX форматах, шаблоны с переменными из CRM данных.

### 8. Файлы:

Стек: Yandex Object Storage (альтернатива: MinIO self-hosted или Cloudflare R2) + boto3 1.35+
Комментарии: Интеграция с AI Studio, локализация данных, хранение документов, медиа, отчетов.

### 9. Мессенджеры:

Стек: aiogram 3.14+ + vk-api 12.0+ + whatsapp-api-client-python 1.5+ + instabot 0.8+
Комментарии: Поддержка всех популярных каналов СНГ, интеграция с CRM и AI-ботами.

### 10.1 CRM и ERP интеграции:

Стек: amoCRM API + Kommo API + RetailCRM API + Bitrix24 API + 1C API
Комментарии: Интеграции с популярными CRM системами, чтение/запись данных, синхронизация статусов, автоматизация бизнес-процессов.

### 10.2 Системы бронирования и календари:

Стек: YCLIENTS API + ALTEGIO API + Google Calendar API + Medfleks API
Комментарии: Интеграции с booking-системами, управление расписанием, автоматическое создание записей через ботов.

### 10.3 Платежи и биллинг:

Стек: WebPay SDK 3.0+ + bePaid SDK 2.5+ + ЕРИП (Беларусь) + AlfaPay + Usage metering
Комментарии: Локальные белорусские платежные системы, интеграция с платформой без сторонних сервисов, отслеживание потребления токенов и лимитов, поддержка ЕРИП для оплаты через интернет-банкинг.

## Frontend

### 1. Фреймворк:

Стек: Next.js 16+ + React 19+ + TypeScript 5.9+
Комментарии: SSR, оптимизация под AI-платформу, расширяемость, типизация.

### 2. Аутентификация:

Стек: NextAuth.js 5.0+ + Yandex OAuth (альтернатива: Auth0 free tier или self-hosted Keycloak)
Комментарии: Готовые провайдеры, enterprise-поддержка, интеграция с backend.

### 3. Состояние/UI:

Стек: TanStack Query 5.90+ + Zustand 5.0+ + Tailwind CSS 4.1+ + shadcn/ui + Zod
Комментарии: Клиентское и серверное состояние, современные компоненты, гибкая стилизация, валидация форм и DTO.

### 4. Админка:

Стек: @adminjs/nextjs 4.0+
Комментарии: Автоматическая генерация админки из моделей данных, быстрое внедрение.

### 5. PWA и мобильные возможности:

Стек: Next.js PWA + Web Push API + Service Workers + Firebase Cloud Messaging
Комментарии: Установка на домашний экран, оффлайн режим, push-уведомления о лимитах и эскалациях, быстрая синхронизация данных.

## Infrastructure:

### 1. Контейнеризация

Стек: Docker 29+ + Docker Compose 2.30+ + Yandex Cloud Container Registry (альтернатива: Docker Hub или self-hosted registry)
Комментарии: Локальная разработка, CI/CD, интеграция с облаком.

### 2. Оркестрация

Стек: Kubernetes 1.35+ + Yandex Managed Service for Kubernetes (альтернатива: k3s self-hosted или minikube)
Комментарии: Горизонтальное масштабирование, высокая доступность, интеграция с облачными сервисами.

### 3. Облако

Стек: Yandex Cloud + Yandex Cloud Functions (serverless) (альтернатива: self-hosted serverless на Kubernetes)
Комментарии: Локализация данных, интеграция AI Studio, соответствие 152-ФЗ, серверless для отдельных задач (RAG обработка, webhook-боты). Yandex Cloud Functions для cost-effective обработки фоновых задач.

### 3.1 Вебхуки и очереди интеграций:

Стек: Redis Queue + Dead Letter Queue + Idempotency keys + Circuit Breaker pattern
Комментарии: Надежная обработка входящих/исходящих вебхуков, дедупликация сообщений, повторные попытки, rate limiting, изоляция сбоев по тенантам.

### 4. Мониторинг и логирование

Стек: Prometheus 3.9+ + Grafana 12.0+ + Sentry 25.12+ + Yandex Cloud Monitoring (альтернатива: self-hosted Prometheus + Grafana)
Комментарии: Метрики, логи, ошибки, дашборды, оповещения.

### 5. CI/CD и автоматизация

Стек: GitHub Actions + Terraform 1.10+ + Ansible 11.0+
Комментарии: Автоматизация деплоя, инфраструктура как код, конфигурационное управление. Terraform для облачной инфраструктуры, Ansible для конфигурации серверов.
