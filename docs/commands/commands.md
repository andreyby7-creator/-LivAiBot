# 📋 Команды проекта core-contracts

## 🔹 Установка зависимостей

```bash
pnpm install                   # Установка всех зависимостей
pnpm install --prod            # Установка только production-зависимостей
pnpm install --frozen-lockfile # Установка с frozen lockfile (CI)
```

## 🔹 Сборка проекта

```bash
pnpm run build       # Полная сборка (JS + типы)
pnpm run build:js    # Только сборка JavaScript
pnpm run build:types # Только сборка типов + форматирование dprint
pnpm run build:watch # Сборка в watch режиме
pnpm run dev         # Dev режим (tsup watch)
```

## 🔹 Проверка кода

```bash
pnpm run type-check # Строгая проверка типов TypeScript
pnpm run clean      # Очистка кэша и временных файлов
```

## 🔹 Линтинг и форматирование

```bash
pnpm run lint:dev                  # Линт в dev режиме (с исправлениями)
pnpm run lint:canary               # Строгий линтинг (canary)
pnpm run lint:ci                   # Линтинг для CI
npx dprint check                   # Проверка форматирования перед коммитом
npx dprint fmt                     # Форматирование всего проекта
npx dprint fmt "src/**/*.{ts,tsx}" # Форматирование только исходного кода (без тестов)
```

## 🔹 Тесты и проверка типов

```bash
pnpm exec vitest run                                     # Запуск всех тестов
pnpm exec vitest run tests/unit/errors/shared/contracts/ # Тесты контрактов
pnpm exec vitest run --typecheck                         # Проверка типов через Vitest
pnpm run test:watch                                      # Тесты в watch режиме
pnpm run test:coverage                                   # Тесты с покрытием
pnpm run test:ci                                         # Тесты для CI с порогами покрытия
```

## 🔹 Вывод результатов

```bash
pnpm exec vitest run --reporter=default             # Обычный вывод тестов
pnpm exec vitest run --reporter=verbose | tail -10  # Подробный вывод тестов (последние 10 строк)
pnpm exec vitest run tests/unit/errors/shared/contracts/ --reporter=verbose --coverage=false | tail -5  # Тесты контрактов без coverage (последние 5 строк)
```

## 🎯 Наиболее используемые команды

### Ежедневная разработка

```bash
pnpm install                      # установка зависимостей
pnpm run dev                      # разработка с watch
pnpm exec vitest run              # запуск тестов
pnpm exec vitest run --typecheck  # проверка типов
pnpm run lint:canary              # линтинг
```

### Перед коммитом

```bash
pnpm run build               # сборка проекта
pnpm run type-check          # строгая проверка типов
pnpm run lint:canary         # финальный линтинг
npx dprint check             # проверка форматирования
pnpm exec vitest run tests/unit/errors/shared/contracts/  # тесты контрактов
```

### CI/CD

```bash
pnpm install --frozen-lockfile # установка для CI
pnpm run test:ci               # тесты с покрытием для CI
pnpm run lint:ci               # линтинг для CI
pnpm run build                 # финальная сборка
```

## 📊 Краткая справка по скриптам

- `build` → Полная сборка (types + js)
- `type-check` → Строгая проверка TS (tsc --noEmit)
- `lint:canary` → Строгий линтинг (canary режим)
- `lint:dev` → Линтинг для разработки (dev режим)
- `test:ci` → Тесты для CI с порогами покрытия
- `clean` → Очистка dist и кэшей

## 📁 Структура проекта

```
core-contracts/
├── src/                   # Исходный код
├── tests/                 # Тесты
├── dist/                  # Скомпилированные файлы
├── docs/                  # Документация
│   └── commands/          # Команды проекта
├── package.json           # Скрипты и зависимости
└── tsconfig.json          # Конфигурация TypeScript
```

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей
pnpm install

# 2. Запуск разработки
pnpm run dev

# 3. В новом терминале - запуск тестов
pnpm exec vitest run

# 4. Проверка типов
pnpm exec vitest run --typecheck
```
