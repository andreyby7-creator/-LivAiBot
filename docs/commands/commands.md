# 📋 Команды проекта LivAiBot

## 🔹 Установка зависимостей

```bash
pnpm install                         # Установка всех зависимостей
pnpm install --prod                  # Установка только production-зависимостей
pnpm install --frozen-lockfile       # Установка с frozen lockfile (CI)
```

## 🚀 Быстрый старт

```bash
pnpm install                         # 1. Установка зависимостей
pnpm run dev                         # 2. Запуск разработки
pnpm exec vitest run                 # 3. В новом терминале - запуск тестов
pnpm exec vitest run --typecheck     # 4. Проверка типов
```

## 🔹 Сборка проекта

```bash
pnpm run build                       # Полная сборка (JS + типы)
pnpm run build:js                    # Только сборка JavaScript
pnpm run build:types                 # Только сборка типов + форматирование dprint
pnpm run build:watch                 # Сборка в watch режиме
pnpm run dev                         # Dev режим (tsup watch)
```

## 🔹 Проверка кода

```bash
pnpm run type-check                  # Строгая проверка типов TypeScript
pnpm run clean                       # Очистка кэша и временных файлов
```

## 🔹 Линтинг и форматирование

```bash
# В корне проекта (для всего монопакета)
pnpm run lint                        # Обычный линтинг через turbo
pnpm run lint:fix                    # Автоисправление через turbo
pnpm run lint:canary                 # Строгий линтинг canary (все пакеты с поддержкой)
pnpm run lint:canary:fix             # Строгий линтинг + автофикс (все пакеты с поддержкой)

# В отдельных пакетах
cd core-contracts && pnpm run lint:dev    # Линт в dev режиме (с исправлениями)
cd core-contracts && pnpm run lint:canary # Строгий линтинг (canary)
cd core-contracts && pnpm run lint:ci     # Линтинг для CI

# Canary режим - низкоуровневые команды (для отдельных файлов/директорий)
ESLINT_MODE=canary npx eslint "src/path/to/file.ts"
ESLINT_MODE=canary npx eslint "src/path/to/dir/"
ESLINT_MODE=canary npx eslint --fix "src/path/to/file.ts"
ESLINT_MODE=canary npx eslint --format=json "src/path/to/dir/" > results.json

# Форматирование
npx dprint check                      # Проверка форматирования перед коммитом
npx dprint fmt                        # Форматирование всего проекта
npx dprint fmt "src/**/*.{ts,tsx}"    # Форматирование только исходного кода (без тестов)
```

## 🔹 Тесты и проверка типов

```bash
# В корне проекта (монопакет)
pnpm run quality                      # Комплексная проверка качества (types + deps)
pnpm run type-coverage                # Анализ покрытия типами (99.94%+)
pnpm run deps:unused                  # Проверка неиспользуемых зависимостей

# В core-contracts/
pnpm run test                         # Запуск всех тестов
pnpm run test:coverage                # Тесты с покрытием
pnpm run type-check                   # Строгая проверка типов
```

## 🔹 Вывод результатов

```bash
pnpm exec vitest run --reporter=default             # Обычный вывод тестов
pnpm exec vitest run --reporter=verbose | tail -10  # Подробный вывод тестов (последние 10 строк)
pnpm exec vitest run tests/unit/errors/shared/contracts/ --reporter=verbose --coverage=false | tail -5  # Тесты контрактов без coverage (последние 5 строк)
```

## 🎯 Наиболее используемые команды

### Ежедневная разработка

```bash
pnpm install                           # установка зависимостей
pnpm run dev                           # разработка с watch
pnpm exec vitest run                   # запуск тестов
pnpm exec vitest run --typecheck       # проверка типов
pnpm run lint:canary                   # линтинг
```

### Контроль качества кода

```bash
# В корне проекта - для всего монопакета
pnpm run quality                       # Полная проверка качества (types + deps)
pnpm run type-coverage                 # Покрытие типами (99.94%+)
pnpm run deps:unused                   # Неиспользуемые зависимости

# В отдельных пакетах
cd core-contracts && pnpm run quality  # Качество core-contracts
```

### Перед коммитом

```bash
pnpm run quality                       # проверка качества кода
pnpm run build                         # сборка проекта
pnpm run lint                          # финальный линтинг
npx dprint check                       # проверка форматирования
pnpm exec vitest run tests/unit/errors/shared/contracts/  # тесты контрактов
```

### CI/CD

```bash
pnpm install --frozen-lockfile         # установка для CI
pnpm run test:ci                       # тесты с покрытием для CI
pnpm run lint:ci                       # линтинг для CI
pnpm run build                         # финальная сборка
```

## 📊 Краткая справка по скриптам

- `build` → Полная сборка (types + js)
- `type-check` → Строгая проверка TS (tsc --noEmit)
- `lint:canary` → Строгий линтинг (canary режим)
- `lint:dev` → Линтинг для разработки (dev режим)
- `test:ci` → Тесты для CI с порогами покрытия
- `clean` → Очистка dist и кэшей

## 📁 Структура проекта

```
livai/
├── api/                   # API слой приложения
├── apps/                  # Приложения (admin-panel, mobile, web, shared-ui)
├── config/                # Конфигурации (eslint, tsconfig, vitest, husky)
├── core-contracts/        # Основные контракты и типы
├── docs/                  # Документация проекта
│   └── commands/          # Команды проекта
├── infrastructure/        # Инфраструктура (docker, k8s, terraform)
├── packages/              # Пакеты/модули проекта
│   ├── domains/           # Домены бизнес-логики
│   ├── fp/                # Функциональное программирование
│   ├── infrastructure-*/  # Инфраструктурные адаптеры
│   └── ui/                # UI компоненты
├── scripts/               # Скрипты проекта
├── package.json           # Корневые зависимости
├── pnpm-workspace.yaml    # Конфигурация workspace
├── turbo.json             # Конфигурация Turbo
└── tsconfig.json          # Корневая конфигурация TypeScript
```
