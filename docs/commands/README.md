# 📚 Commands Documentation

Эта директория содержит документацию по командам проекта core-contracts.

## 📄 Файлы

- [`commands.md`](./commands.md) - Полный справочник команд для разработки, тестирования и сборки проекта

## 🎯 Кратко о командах

### Быстрый старт

```bash
pnpm install                    # установка зависимостей
pnpm run dev                   # разработка
pnpm exec vitest run          # тесты
pnpm exec vitest run --typecheck  # проверка типов
```

### Перед коммитом

```bash
pnpm run build                # сборка
pnpm run type-check          # типы
pnpm run lint:canary         # линтинг
npx dprint check             # форматирование
```

Смотрите подробности в [`commands.md`](./commands.md).
