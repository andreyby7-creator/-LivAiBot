# Zod Generator Implementation

## КОНТЕКСТ ПРОЕКТА

### 1. Архитектура (LivAi-Structure.md)

- Hexagonal architecture с domain/ports/use_cases/entrypoints/adapters
- Domain-driven design с чистыми доменными моделями
- Строгая типизация на всех уровнях
- Dependency inversion через ports/adapters

### 2. Текущая реализация (phase0-1-backend.md)

- Pydantic v2 с валидацией (Field constraints, field_validator)
- TypeScript типы в core-contracts без runtime валидации
- Много сервисов: auth, bots, conversations, knowledge, integrations
- Enterprise уровень: аудит, безопасность, мульти-тенантность

### 3. Будущие требования (Roadmap + Overview)

- Фазы 3-7: CRM/ERP интеграции, маркетплейсы, booking системы, AI/RAG
- 50+ внешних API для интеграций
- Сложные формы: мультиагентность, conditional валидация, i18n
- Real-time: WebSocket, SSE, push notifications
- Enterprise: аудит, compliance, масштабируемость

### 4. Tech Stack (LivAi-Tech-Stack.md)

- FastAPI + Pydantic в backend
- TypeScript + Effect в frontend
- Много интеграций: CRM, мессенджеры, платежи, AI
- Yandex Cloud экосистема

## 🔍 ПРОБЛЕМАТИКА ВАЛИДАЦИИ

### Текущие вызовы

- Синхронизация: Backend Pydantic ↔ Frontend TypeScript типы
- Runtime валидация: Frontend формы без проверки данных
- Масштаб: 50+ DTO в будущем с интеграциями
- Enterprise: Аудит, безопасность, compliance
- Сложность: Conditional валидация, i18n, feature flags

### Будущие риски без Zod

- Рассинхронизация API контрактов
- Проблемы UX в формах без валидации
- Безопасность без runtime проверки
- Производительность поддержки при росте

## 📊 СРАВНЕНИЕ ПОДХОДОВ (В КОНТЕКСТЕ ПРОЕКТА)

| Критерий         | Ручная Zod | Автогенерация | Гибридный |
| ---------------- | ---------- | ------------- | --------- |
| Синхронизация    | Средняя    | Отличная      | Отличная  |
| Контроль         | Отличный   | Минимальный   | Хороший   |
| Масштабируемость | Низкая     | Высокая       | Высокая   |
| Enterprise       | Средняя    | Высокая       | Высокая   |
| Frontend UX      | Отличный   | Средний       | Отличный  |
| Поддержка        | Высокая    | Низкая        | Средняя   |

## 🎯 РЕКОМЕНДАЦИЯ: ГИБРИДНЫЙ ПОДХОД

### Почему именно гибридный

- Enterprise масштаб: 50+ API, сложные интеграции, аудит
- Быстрое развитие: Частые изменения API в фазах 3-7
- Frontend UX: Сложные формы с conditional валидацией
- Безопасность: Runtime валидация критических данных
- Архитектура: Соответствует hexagonal принципам

### Роли валидации (чтобы не дублировать backend)

- **Backend**: продолжает валидировать входные данные через **Pydantic** (это источник истины для server-side валидации).
- **Zod**: это **frontend/runtime guard** (формы/клиент/edge), а не замена backend-валидации.

### Техническая реализация

**📁 Структура: `packages/core-contracts/src/validation/zod/`**

```
packages/core-contracts/src/validation/zod/
├── generated/           # Автогенерированные схемы из OpenAPI
│   ├── auth.ts         # RegisterRequest, LoginRequest, etc.
│   ├── bots.ts         # BotCreateRequest, BotResponse, etc.
│   ├── conversations.ts # ThreadCreateRequest, MessageResponse, etc.
│   ├── knowledge.ts    # Для фазы 3 (RAG)
│   └── integrations.ts # Для фаз 4+ (CRM, маркетплейсы)
├── custom/             # Ручные расширения
│   ├── forms.ts        # confirmPassword, complex validations
│   ├── conditional.ts  # feature-flag валидации
│   └── i18n.ts         # мультиязычные сообщения
├── utils/              # Вспомогательные функции
│   ├── transform.ts    # Конвертация Pydantic → Zod
│   └── validate.ts     # Кастомные валидаторы
└── index.ts            # Единый экспорт всех схем
```

**🛠️ Генераторы (живут в корне репозитория)**

```
scripts/
├── generate-openapi-snapshots.py     # FastAPI app.openapi() → services/<service>/openapi.json
├── generate-zod-schemas.mjs          # OpenAPI → packages/core-contracts/.../generated/*.ts
└── templates/openapi-zod-schemas-only.hbs
```

### Правило: `generated/**` нельзя редактировать вручную

❗ **Файлы в `generated/` нельзя править руками** — любые изменения идут только через OpenAPI (и запуск генерации).

Практики защиты:

- В каждый сгенерированный файл добавляем баннер:

```ts
/* ⚠️ AUTO-GENERATED. DO NOT EDIT. */
```

- (Опционально) добавить проверку в lint/CI: запрет правок `generated/**` вне команды генерации.

**🔧 Технический стек**

- Генератор: openapi-zod-client **~1.18.3** + кастомные трансформеры
- Zod: **4.1.12/4.1.10** для runtime валидации
- zod-openapi: **~2.16.0 (опционально)** — нужно только если потребуется _генерировать OpenAPI из Zod_ (Zod → OpenAPI). В текущем потоке (OpenAPI → Zod) обычно не обязателен.
- Backend: FastAPI **0.128.0** с OpenAPI генерацией
- Интеграция: Turborepo task для генерации в CI/CD
- Расширение: Zod .extend() для frontend-специфичной логики

### Стратегия генерации (важно для CI/CD)

- **Generated схемы коммитим в репозиторий** (`src/validation/zod/generated/**`), чтобы сборка фронта не зависела от доступности backend на этапе установки/сборки.
- В CI добавляем шаг **проверки актуальности генерации**: выполнить генерацию и убедиться, что `git diff` пуст (иначе падаем с ошибкой).

### Источник OpenAPI (должно быть детерминировано)

Один из вариантов (выбрать и зафиксировать в репо):

- **OpenAPI snapshots в репозитории** (например `services/*/openapi.json`) → генератор читает эти файлы
- или **скрипт, который детерминированно генерит `openapi.json` из FastAPI** без запуска сервера (import приложения и `app.openapi()`)

### Примеры использования

```typescript
// Автогенерированная базовая схема
export const RegisterRequestSchema = generatedSchemas.RegisterRequest;

// Frontend расширение с кастомной валидацией
export const RegisterFormSchema = RegisterRequestSchema.extend({
  confirmPassword: z.string().min(8),
  acceptTerms: z.boolean().refine((val) => val === true, {
    message: 'Необходимо принять условия',
  }),
}).refine((data) => data.password === data.confirmPassword, {
  message: 'Пароли не совпадают',
  path: ['confirmPassword'],
});

// Feature-flag валидация
export const BotCreateSchema = generatedSchemas.BotCreate.extend({
  advancedSettings: featureFlags.aiEnabled
    ? z.object({ temperature: z.number().min(0).max(2) }).optional()
    : z.never(),
});
```

## 🚀 ПЛАН РЕАЛИЗАЦИИ

### Этап 1: Базовая инфраструктура

- Настроить OpenAPI генерацию в FastAPI сервисах и зафиксировать источник OpenAPI (snapshots/скрипт)
- Создать структуру `packages/core-contracts/src/validation/zod/`
- Интегрировать openapi-zod-client в CI/CD
- Коммитить `generated/**` и добавить CI проверку актуальности (генерация + `git diff --exit-code`)

### Этап 2: Генерация базовых схем

- Автогенерация для auth, bots, conversations
- Тестирование синхронизации с backend

### Этап 3: Frontend расширения

- Ручные схемы для форм (confirmPassword, complex validations)
- Conditional валидация с feature flags
- i18n сообщения об ошибках

### Этап 4: Интеграция в UI (фаза 2)

- React Hook Form + Zod интеграция
- Error handling с Zod errors
- Type-safe формы с автокомплитом

### Этап 5: Масштабирование (фазы 3-7)

- Автогенерация для новых интеграций (CRM, маркетплейсы)
- Enterprise валидации (PII, compliance)
- Performance оптимизации
- (Опционально) **Zod → OpenAPI/JSON Schema для интеграторов**:
  - если появится “developer-facing” слой (публичный API/SDK, marketplace действий, пользовательские webhook/actions со схемами)
  - тогда рассмотреть `zod-openapi` (для OpenAPI) или альтернативно генерацию JSON Schema из Zod (если нужен контракт схем, а не весь OpenAPI)

## ✅ ПРЕИМУЩЕСТВА ВЫБРАННОГО ПОДХОДА

- Синхронизация: 100% соответствие backend API
- Гибкость: Возможность кастомизации для UX
- Масштабируемость: Автоматическая генерация при росте
- Безопасность: Runtime валидация всех входных данных + Snyk проверка зависимостей
- Производительность: Type-safe на всех уровнях
- Поддержка: Enterprise-ready с аудит и compliance
- Совместимость: OpenAPI v3.x поддержка через zod-openapi

## 📋 Чеклист внедрения Zod генератора

### ✅ Этап 0: Подготовка — РЕАЛИЗОВАН

- [x] Обновить FastAPI сервисы до версии **0.128.0** с корректной OpenAPI генерацией
- [x] Проверить версии зависимостей:
  - Zod **4.1.12/4.1.10** (самый свежий стабильный патч)
  - openapi-zod-client **~1.18.3** (последняя версия согласно Snyk, нет критичных уязвимостей)
  - zod-openapi **~2.16.0 (опционально)** — только если потребуется Zod → OpenAPI
  - TypeScript strict mode
  - FastAPI **0.128.0**
- [x] Создать папку: `packages/core-contracts/src/validation/zod/`

### ✅ Этап 1: Базовая инфраструктура — РЕАЛИЗОВАН

- [x] Определить и зафиксировать источник OpenAPI:
  - snapshots `openapi.json` в репозитории (рекомендуется для воспроизводимости), или
  - скрипт генерации `openapi.json` из FastAPI (детерминированно, без запуска сервера)
- [x] Настроить генерацию OpenAPI JSON для всех сервисов: auth, bots, conversations
- [x] Настроить Turborepo task для генерации Zod схем из OpenAPI
- [x] Создать структуру:
  ```
  packages/core-contracts/src/validation/zod/
  ├── generated/
  ├── custom/
  ├── utils/
  └── index.ts
  ```
- [x] Стратегия: **коммитить `generated/**`** в репозиторий
- [x] Настроить CI/CD: генерация + проверка актуальности (`git diff --exit-code`) при изменении OpenAPI / backend API

### ✅ Этап 2: Генерация базовых схем — РЕАЛИЗОВАН

- [x] Сгенерировать схемы для auth, bots, conversations
- [x] Проверить:
  - Совпадение типов с backend DTO (источник истины: backend OpenAPI snapshots)
  - Корректная работа `.parse()` для всех полей (unit smoke для всех схем + кейсы с ограничениями)
  - Обработка ошибок валидации (unit кейсы `.safeParse()` + наличие issues)
  - Проверка зависимостей (CI: `pnpm audit`; опционально локально: `pnpm run security:snyk`)
- [x] Добавить базовые unit-тесты для схем (`.parse()` + ошибки)

### ✅ Этап 3: Ручные расширения (Frontend) — РЕАЛИЗОВАН

- [x] Создать ручные схемы в `custom/`:
  - `forms.ts` → confirmPassword, acceptTerms, complex validations
  - `conditional.ts` → feature flags
  - `i18n.ts` → мультиязычные ошибки
- [x] Использовать `.extend()` для объединения автогенерации и кастомной логики
- [x] Написать unit-тесты для conditional + i18n логики
- [x] Интегрировать с Effect для side effects валидации

### ✅ Этап 4: Интеграция в UI (Фаза 2) — РЕАЛИЗОВАН

**Когда реализуем:** в рамках **Фазы 2 (UI)** — стартуем после завершения **Фазы 1** (контракты API).

- [x] Подключить схемы к React Hook Form:
  - schemas: `packages/feature-auth/src/schemas.ts` (на базе `@livai/core-contracts/validation/zod`)
  - формы: `packages/ui-features/src/auth/*-form.tsx`
  - компоновка страниц: `apps/web/src/app/[locale]/auth/*/page.tsx`
  - resolver: `packages/ui-shared/src/validation/rhf-zod-resolver.ts`

  ```typescript
  useForm({ resolver: zodResolver(schema) });
  ```
- [x] Настроить централизованную обработку ошибок:
  - i18n: `apps/web/messages/{en,ru}.json` + `next-intl` (`apps/web/i18n/*`, `apps/web/middleware.ts`)
  - отображение ошибок: через `FormField` (`packages/ui-core`) + перевод ключа в UI (`translateZodMessage`)
- [x] Проверить type-safety:
  - автокомплит: `z.infer<typeof schema>` в feature/auth и ui-features
  - runtime guard: `schema.safeParse/parse` внутри resolver (Zod v4)

### Этап 5: Масштабирование (Фазы 3–7)

**Когда реализуем (по Roadmap):**

- **Фаза 3 (Knowledge/RAG)**: добавить сервис(ы) в pipeline (OpenAPI snapshots + Zod генерация) и завести `generated/knowledge.ts`.
- **Фаза 4 (Интеграции/вебхуки)**: добавить интеграционные эндпоинты/сервисы в pipeline и завести `generated/integrations.ts`.
- **Фаза 5 (Security/Team/API Keys)**: расширять схемы под новые DTO (roles/API keys) и при необходимости готовить контрактный слой для интеграторов (опционально).
- **Фазы 6–7**: точечно добавлять enterprise-валидации/ограничения по мере появления DTO (billing/limits/observability).

- [ ] Автогенерация новых схем при добавлении сервисов:
  - knowledge, integrations (CRM, маркетплейсы, AI)
- [ ] Добавить enterprise-валидации:
  - PII fields
  - Compliance rules
  - Security validations
- [ ] (Опционально, ближе к фазам интеграций/API keys) Если потребуется “контракт для интеграторов”:
  - Zod → OpenAPI (`zod-openapi`) или Zod → JSON Schema (альтернатива)
  - генерация спецификаций/SDK для публичного developer API, marketplace действий, user-defined webhooks/actions
- [ ] Оптимизация производительности генератора:
  - Incremental generation
  - Кеширование схем
  - Lazy loading больших схем
- [ ] Поддержка feature flags и complex forms для новых интеграций
- [ ] Добавить E2E тесты для валидации форм с реальными данными

## 🎯 ИТОГ

Нужен гибридный Zod генератор в `packages/core-contracts/src/validation/zod/` с:

- Автогенерацией базовых схем из OpenAPI (FastAPI 0.128.0 + openapi-zod-client ~1.18.3)
- Zod 4.1.12/4.1.10 для runtime валидации
- zod-openapi ~2.16.0 для OpenAPI v3.x интеграции
- Ручными расширениями для frontend UX
- Conditional валидацией с feature flags
- i18n поддержкой для ошибок
- Snyk проверка безопасности зависимостей
