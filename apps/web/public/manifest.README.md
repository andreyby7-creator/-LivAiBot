# 📱 PWA Manifest для LivAi Platform

## 📋 Описание

Web App Manifest (manifest.json) — конфигурационный файл для Progressive Web Application (PWA), который определяет метаданные приложения, иконки, цвета темы, быстрые действия и другие параметры для установки на домашний экран устройства.

## 🎯 Назначение

Этот манифест обеспечивает:

- **Установку PWA** на домашний экран (Add to Home Screen)
- **Standalone режим** отображения (без адресной строки браузера)
- **Быстрые действия** (shortcuts) для быстрого доступа к ключевым функциям
- **Иконки** для различных размеров экранов и платформ
- **Цвета темы** для системного UI (status bar, navigation bar)

## 📁 Структура файла

### Основные поля

- **`id`** — Стабильный идентификатор приложения (`/?app=livai`) для различения версий PWA
- **`name`** — Полное название приложения (отображается при установке)
- **`short_name`** — Короткое название (для ограниченного пространства)
- **`description`** — Описание функциональности приложения
- **`start_url`** — URL точки входа с tracking параметром (`/?source=pwa`) для аналитики
- **`scope`** — Область действия PWA (какие URL обрабатываются)
- **`display`** — Режим отображения (`standalone` — как нативное приложение)
- **`display_override`** — Fallback режимы для совместимости (Windows PWA, Chrome OS)
- **`orientation`** — Предпочтительная ориентация (`any` — поддерживает все ориентации)
- **`theme_color`** — Цвет темы для системного UI (#2563eb — синий)
- **`background_color`** — Цвет фона при загрузке (#ffffff — белый)
- **`lang`** — Язык манифеста (`ru-RU` — русский с регионом)

### Иконки (icons)

**Оптимизированный порядок** для быстрого выбора браузером (best practice):

**Приоритетные (первыми):**

- **192x192** (any + maskable) — основной размер для Android
- **512x512** (any + maskable) — для splash screen и высоких разрешений

**Вторичные (ниже):**

- **72x72, 96x96, 128x128** — для старых Android устройств
- **144x144, 152x152** — для iOS и Windows
- **384x384** — для Android (Chrome)

**Purpose:**

- `any` — стандартные иконки (отдельные файлы)
- `maskable` — адаптивные иконки с безопасной зоной 20% (отдельные файлы: `icon-192-maskable.png`, `icon-512-maskable.png`)

**Важно:**

- Maskable иконки должны быть в отдельных файлах согласно рекомендациям Google
- Порядок оптимизирован: сначала критичные размеры (192, 512), потом остальные

### Быстрые действия (shortcuts)

Три быстрых действия согласно требованиям LivAiBot-Specs:

1. **Диалоги** — `/dashboard/dialogs` — открытие списка диалогов
2. **Проверить лимиты** — `/dashboard/billing` — просмотр лимитов и использования
3. **Тест-чат** — `/dashboard/chat/test` — тестовый чат для проверки бота

### Скриншоты (screenshots)

Обязательные скриншоты для повышения install-conversion:

- **mobile-dashboard.png** (390x844) — мобильная версия dashboard
- **desktop-dashboard.png** (1920x1080) — десктопная версия dashboard

### Обработчики протоколов (protocol_handlers)

Кастомный протокол `web+livai://` для открытия приложения по специальным ссылкам:

- **Протокол:** `web+livai`
- **URL обработчик:** `/open?url=%s`

**Использование:** `web+livai://bot/123` → откроет `/open?url=bot/123`

### Обработчики файлов (file_handlers)

Поддержка открытия файлов напрямую в PWA:

- **JSON** (`.json`) — для импорта конфигураций ботов, датасетов
- **CSV** (`.csv`) — для импорта данных, списков
- **TXT** (`.txt`) — для текстовых файлов, баз знаний

**Действие:** `/import` — обработка загруженных файлов

### Обработчик запуска (launch_handler)

Предотвращает дублирование окон PWA:

- **`client_mode: "focus-existing"`** — фокусирует существующее окно вместо создания нового

### Edge Side Panel (edge_side_panel)

**Мощная фича для AI-платформы** — позволяет LivAi работать как AI-ассистент сбоку браузера Chrome:

- **`preferred_width: 400`** — оптимальная ширина панели для чата и быстрых действий
- **Поддержка:** Chrome 120+ (Edge, Chrome)
- **Использование:** Пользователь может открыть LivAi в боковой панели браузера для быстрого доступа к AI-ботам

**Преимущества:**

- Непрерывная работа AI-ассистента во время просмотра других сайтов
- Быстрый доступ к чату и функциям без переключения вкладок
- Идеально для AI-платформы с постоянным взаимодействием

## 🔄 Интеграция с i18n

**Текущая реализация:** Статический манифест с русским языком (`ru-RU`) по умолчанию.

**Архитектурное решение:**

- `lang: "ru-RU"` — базовый язык манифеста с регионом
- Мультиязычность реализуется через метаданные в `layout.tsx` (next-intl)
- Имена и описания в манифесте на русском (основной рынок)

**Будущее улучшение:** Для полной поддержки i18n (ru/en) можно:

1. **Динамическая генерация** через Next.js route handler (`/manifest.json`)
2. **Отдельные манифесты** для каждой локали (`manifest.ru.json`, `manifest.en.json`)
3. **Генерация на build-time** с использованием next-intl

## 🛠️ Требования к иконкам

Все иконки должны быть размещены в `/public/icons/`:

```
public/
  icons/
    icon-192x192.png           ← приоритетный размер (первым в манифесте)
    icon-192-maskable.png      ← отдельный файл для maskable
    icon-512x512.png           ← приоритетный размер (вторым в манифесте)
    icon-512-maskable.png      ← отдельный файл для maskable
    icon-72x72.png             ← вторичные размеры
    icon-96x96.png
    icon-128x128.png
    icon-144x144.png
    icon-152x152.png
    icon-384x384.png
    shortcut-dialogs-96x96.png
    shortcut-billing-96x96.png
    shortcut-chat-96x96.png
  screenshots/
    mobile-dashboard.png        ← 390x844 (первым в манифесте)
    desktop-dashboard.png      ← 1920x1080 (вторым в манифесте)
```

**Рекомендации:**

- Формат: PNG с прозрачностью
- Maskable иконки: **отдельные файлы** с безопасной зоной 20% от краёв (для Android)
- Оптимизация: использовать инструменты типа `sharp` или `imagemin`
- Скриншоты: реальные скриншоты приложения (не моки) для лучшей конверсии

## 🔗 Подключение в Next.js

Манифест автоматически подхватывается Next.js из `/public/manifest.json`.

Для явного подключения в `layout.tsx`:

```tsx
export const metadata: Metadata = {
  manifest: '/manifest.json',
  // ...
};
```

## 📱 Тестирование

1. **Chrome DevTools** → Application → Manifest
2. **Lighthouse** → PWA audit
3. **Web App Manifest Validator**: https://manifest-validator.appspot.com/

## 🚀 Best Practices

- ✅ Все обязательные поля заполнены
- ✅ **ID приложения** для стабильной идентификации версий
- ✅ **Tracking параметр** в start_url (`?source=pwa`) для аналитики
- ✅ **Display override** для совместимости с Windows PWA / Chrome OS
- ✅ **Оптимизированный порядок иконок** (192, 512 первыми для быстрого выбора)
- ✅ Иконки всех необходимых размеров
- ✅ **Отдельные maskable иконки** (Google рекомендация)
- ✅ **Purpose: any** в иконках shortcuts для полноты
- ✅ Быстрые действия для улучшения UX
- ✅ **Скриншоты** (mobile первый, desktop второй) для повышения install-conversion
- ✅ **Protocol handlers** для кастомных ссылок (`web+livai://`)
- ✅ **File handlers** для импорта файлов (JSON, CSV, TXT)
- ✅ **Launch handler** для предотвращения дублирования окон
- ✅ **Edge Side Panel** для работы как AI-ассистент в браузере (Chrome 120+)
- ✅ **Orientation: any** для поддержки всех устройств
- ✅ **Расширенное название** с продающим сообщением ("AI-ботов и AI-сотрудников")
- ✅ Правильные цвета темы
- ✅ Standalone режим для нативного опыта
- ✅ Расширенные категории (AI, automation, communication)

## 📚 Ссылки

- [Web App Manifest Specification](https://www.w3.org/TR/appmanifest/)
- [MDN: Web App Manifests](https://developer.mozilla.org/en-US/docs/Web/Manifest)
- [PWA Checklist](https://web.dev/pwa-checklist/)
