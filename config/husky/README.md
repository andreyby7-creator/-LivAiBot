# 🔗 Husky - Git Hooks для LivAI

## 📋 Обзор

Husky обеспечивает автоматическое выполнение скриптов качества кода на различных этапах Git workflow. Все hooks запускаются локально перед коммитом/пушем, обеспечивая высокое качество кода.

## 🪝 Настроенные Hooks

### `commit-msg` - Валидация сообщений коммитов
**Назначение:** Проверяет формат commit messages согласно Conventional Commits
**Формат:** `type(scope): description`
**Типы:** feat, fix, docs, style, refactor, test, chore

### `pre-commit` - Быстрые проверки перед коммитом
**Назначение:** Легковесные проверки на staged файлах
**Проверки:**
- ESLint + Prettier (lint-staged)
- Type checking измененных пакетов
- Быстрые тесты измененных пакетов
- Infrastructure verification при необходимости

### `pre-push` - Полные проверки перед пушем
**Назначение:** Комплексные проверки перед отправкой в remote
**Проверки:**
- Полная type checking всех пакетов
- Полный ESLint всех файлов
- Запуск всех тестов
- Архитектурная валидация
- Infrastructure verification

## ⚙️ Конфигурация

### lint-staged
Автоматически запускает линтинг и форматирование только на staged файлах:

```json
{
  "*.{js,jsx,ts,tsx}": ["eslint --fix", "prettier --write"],
  "*.{json,md,yml,yaml}": ["prettier --write"]
}
```

### Установка и активация

```bash
# Инициализация git репозитория
git init

# Установка hooks
pnpm run prepare

# Hooks активируются автоматически
```

## 🚀 Работа с Hooks

### Режимы работы

#### Development (обычная работа)
- **pre-commit:** Быстрые проверки на staged файлах
- **pre-push:** Полные проверки перед отправкой

#### CI/CD (строгий режим)
- Все hooks работают в полную силу
- Любая ошибка останавливает процесс

### Отключение hooks (только в экстренных случаях)

```bash
# Отключить все hooks
git commit --no-verify

# Или отключить конкретный hook
HUSKY_SKIP_HOOKS=1 git commit
```

## 📊 Производительность

### Pre-commit (быстрый)
- ⏱️ **Время:** 10-30 сек
- 🎯 **Scope:** Только staged файлы
- ✅ **Проверки:** Линтинг, типы, тесты

### Pre-push (полный)
- ⏱️ **Время:** 2-5 мин
- 🎯 **Scope:** Весь проект
- ✅ **Проверки:** Все доступные тесты и валидации

## 🔧 Кастомизация

### Добавление новых проверок

1. **В pre-commit hook:**
   ```bash
   # Добавить после существующих проверок
   echo "  🔍 Running custom check..."
   pnpm run custom:check
   ```

2. **В pre-push hook:**
   ```bash
   # Добавить после существующих проверок
   echo "  🔍 Running custom validation..."
   pnpm run custom:validate
   ```

### Изменение порогов

```bash
# Изменить таймауты в hooks
# Изменить количество параллельных процессов
# Настроить специфические проверки для пакетов
```

## 🎯 Лучшие практики

### Для разработчиков
- ✅ **Всегда используйте `git add -p`** для точного staging
- ✅ **Проверяйте hooks локально** перед пушем
- ✅ **Используйте descriptive commit messages**

### Для CI/CD
- ✅ **Hooks обеспечивают baseline quality**
- ✅ **Дополнительные проверки в CI** (coverage, e2e)
- ✅ **Автоматический rollback** при проблемах

## 🐛 Устранение проблем

### Hook не запускается
```bash
# Проверить права на исполнение
ls -la .husky/

# Переинициализировать hooks
pnpm run prepare
```

### Hook слишком медленный
```bash
# Оптимизировать lint-staged конфигурацию
# Добавить .eslintignore/.prettierignore
# Использовать параллельные проверки
```

### Ложные срабатывания
```bash
# Проверить .eslintignore/.prettierignore
# Использовать eslint-disable комментарии
# Проверить специфические правила
```

---

*Husky обеспечивает zero-config quality gates для всего LivAI проекта!* 🎉
