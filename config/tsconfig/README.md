# 🔷 TypeScript - Система конфигураций LivAI

## 📋 Обзор

Иерархическая система конфигураций **TypeScript** для монорепозитория LivAI. Обеспечивает
максимальную типобезопасность, производительность компиляции и масштабируемость проекта.

## 🏗️ Архитектура конфигураций

### Иерархическая структура

```
config/tsconfig/
├── base.json           # Универсальные базовые настройки
├── root.json           # Корневая конфигурация проекта
├── node.json           # Node.js ESM конфигурация
├── strict.json         # Максимально строгие проверки (Node.js)
├── test-configs.json   # Специфично для тестов Vitest
└── README.md          # Эта документация

tsconfig.json           # Корневой конфиг проекта (в корне)
```

### Описание файлов

#### `base.json` - Универсальная базовая конфигурация

**Назначение:** Фундаментальные настройки TypeScript для фронта и бэкенда **Функции:**

- Современный стандарт ES2024 для компиляции
- Полная строгая типизация + 10 дополнительных проверок
- Оптимизированные path mappings (@livai/_, @/_)
- React, Node.js и DOM интеграция
- Bundler module resolution для универсальности

#### `root.json` - Корневая конфигурация проекта

**Назначение:** Основная конфигурация для исходного кода проекта **Функции:**

- Полный охват всех приложений и пакетов
- Исключение артефактов сборки и зависимостей
- Оптимизированные паттерны включения/исключения
- Поддержка apps, api, core-contracts, packages

#### `node.json` - Node.js ESM конфигурация

**Назначение:** Специализированная конфигурация для Node.js сред **Функции:**

- NodeNext модули и разрешение
- Verbatim module syntax для ESM
- Фокус на backend коде (packages, core-contracts, api)
- Оптимизация для серверного JavaScript

#### `strict.json` - Строгая Node.js конфигурация

**Назначение:** Максимальная типобезопасность для Node.js кода **Функции:**

- Обязательное использование всех переменных/параметров
- Запрет небезопасного доступа к индексам
- NodeNext модульная система
- Полная проверка backend и mobile приложений

#### `test-configs.json` - Тестовая конфигурация

**Назначение:** Специализированная конфигурация для Vitest конфигурационных файлов **Функции:**

- Фокус только на config/vitest/\*_/_ файлах
- Исключение coverage и результатов тестирования
- Минимальный scope для быстрой проверки типов

### Принципы наследования

- **Базовая конфигурация** - универсальные настройки для фронта/бэкенда
- **Специализированные оверрайды** - дополнительные правила для конкретных сред
- **Гибкость уровней** - разные степени строгости для разных нужд

## 🎯 Конфигурационные уровни

### Base (Универсальный базовый)

**Файл:** `base.json` **Назначение:** Фундаментальные настройки для всего проекта

**Ключевые настройки:**

- Современный стандарт **ES2024** для компиляции
- Полная строгая типизация + 10 дополнительных проверок
- Оптимизированные path mappings (@livai/_, @/_)
- React, Node.js и DOM интеграция
- Bundler module resolution

### Root (Корневой проектный)

**Файл:** `root.json` **Назначение:** Основная конфигурация для исходного кода

**Особенности:**

- Полный охват всех исходных файлов
- Исключение артефактов сборки
- Поддержка всех приложений и пакетов
- Оптимизированные include/exclude паттерны

### Node (Node.js ESM)

**Файл:** `node.json` **Назначение:** Специализированная конфигурация для Node.js сред

**Ключевые настройки:**

- **Module:** NodeNext для нативной Node.js ESM
- **ModuleResolution:** NodeNext для правильного разрешения
- **VerbatimModuleSyntax:** Чистый import/export без трансформаций
- Фокус на backend коде (packages, core-contracts, api)

### Strict (Строгий Node.js)

**Файл:** `strict.json` **Назначение:** Максимальная типобезопасность для Node.js кода

**Дополнительные проверки:**

- Обязательное использование всех переменных/параметров
- Запрет небезопасного доступа к индексам объектов
- NodeNext модульная система
- Полная проверка backend и mobile приложений

### Test Configs (Тестовая)

**Файл:** `test-configs.json` **Назначение:** Специфическая конфигурация для Vitest конфигов

**Особенности:**

- Только config/vitest/\*_/_ файлы
- Минимальный scope для скорости
- Исключение coverage и результатов тестов

## ⚙️ Основные возможности

### Современные стандарты

- **ES2024 target** - новейшие возможности JavaScript
- **ES2022 modules** - совместимость с экосистемой
- **Strict mode** - нулевая терпимость к ошибкам

### Path Mapping

Интеллектуальная система импортов для LivAI архитектуры:

- `@livai/core-contracts` - основной инфраструктурный пакет
- `@livai/core-contracts/*` - модули core-contracts (auth, domain, etc.)
- `@livai/domains/*` - доменные модели и бизнес-логика
- `@livai/infrastructure/*` - инфраструктурные адаптеры
- `@livai/fp/*` - функциональное программирование утилиты
- `@livai/application-core` - приложениеядро
- `@livai/data` - слой данных и Prisma
- `@livai/*` - остальные пакеты (sdk, security, etc.)
- `@/*` - app-специфичные импорты

### Производительность

- **Инкрементальная компиляция** - только измененные файлы
- **Оптимизированные исключения** - минимум проверок для артефактов
- **Параллельная обработка** - использование всех ядер

## 🔧 Продвинутые возможности

### Type Checking

- **10 уровней строгой типизации** - от базовых до экспертных
- **Exact optional properties** - точные опциональные поля
- **Unknown in catch** - безопасная обработка ошибок

### Архитектурная поддержка

- **Изоляция модулей** - предотвращение циклических зависимостей
- **Declaration maps** - навигация по типам в IDE
- **Source maps** - отладка с оригинальным кодом

### Масштабируемость

- **Неограниченное количество приложений** - автоматическое обнаружение
- **Гибкие исключения** - адаптация под специфические нужды
- **Наследование настроек** - легкое расширение

## 📊 Краткое сравнение конфигов

- **`base.json`**
  - ⚡ **Скорость:** Быстрая компиляция
  - 🟢 **Строгость:** Средняя (универсальная база)
  - 🎯 **Назначение:** Фундамент для фронтенда и бэкенда

- **`root.json`**
  - ⚡ **Скорость:** Быстрая компиляция
  - 🟢 **Строгость:** Средняя (наследует от base)
  - 🎯 **Назначение:** Полный охват исходного кода проекта

- **`node.json`**
  - ⚡ **Скорость:** Быстрая компиляция
  - 🟢 **Строгость:** Средняя (Node.js оптимизация)
  - 🎯 **Назначение:** Разработка Node.js приложений с ESM

- **`strict.json`**
  - 🟡 **Скорость:** Средняя (дополнительные проверки)
  - 🔴 **Строгость:** Максимальная типобезопасность
  - 🎯 **Назначение:** CI/CD пайплайны и финальная проверка

- **`test-configs.json`**
  - ⚡ **Скорость:** Быстрая компиляция
  - 🟢 **Строгость:** Минимальная (оптимизация для тестов)
  - 🎯 **Назначение:** Проверка типов Vitest конфигураций

## 🎯 Ключевые преимущества LivAI TypeScript системы

- **🏢 Enterprise-grade типобезопасность** - от стартапа до корпорации
- **⚡ Современная производительность** - ES2024 + оптимизированные конфиги
- **🔧 Гибкая архитектура** - поддержка фронтенда, бэкенда и мобильных приложений
- **📦 Умное разрешение модулей** - path mapping для сложной монорепозиторий структуры
- **🚀 Автоматизированное качество** - CI/CD готовые строгие проверки

## 🚀 Быстрый старт

### Использование конфигов:

```bash
# Основная разработка
npx tsc --project tsconfig.json

# Node.js разработка
npx tsc --project config/tsconfig/node.json

# Строгая проверка для CI/CD
npx tsc --project config/tsconfig/strict.json

# Проверка Vitest конфигов
npx tsc --project config/tsconfig/test-configs.json
```

### Path mapping примеры:

```typescript
// Импорт из core-contracts
import { Version } from '@livai/core-contracts';

// Импорт из domains
import { User } from '@livai/domains/domains-users';

// Импорт из инфраструктуры
import { Database } from '@livai/infrastructure/infrastructure-core';

// App-специфичный импорт
import { Component } from '@/components/Button';
```

---

_TypeScript конфигурации LivAI обеспечивают типобезопасность с современной производительностью и
масштабируемостью для фронтенда и бэкенда._
