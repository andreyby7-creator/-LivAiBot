/**
 * @file @livai/core-contracts/domain — Domain Types & DTO
 * Публичный API пакета domain.
 * Экспортирует все публичные компоненты, типы и DTO для всех доменов.
 * Включает common типы, app-effects типы, auth типы, bots типы, conversations типы, telemetry типы.
 */

/* ============================================================================
 * 🧩 ТИПЫ — COMMON DOMAIN TYPES
 * ============================================================================
 */

/**
 * Общие типы, используемые во всех доменах.
 * Включает UUID, ISODateString, JsonObject, Settings, Decision types.
 * @public
 */
export * from './common.js';

/* ============================================================================
 * ⚡ ТИПЫ — APP EFFECT & ERROR CONTRACTS
 * ============================================================================
 */

/**
 * App-level shared effect & error contracts.
 * Включает ApiRequestContext/ApiError/ApiResponse, AppError и связанные типы.
 * Используются core effect-слоем и error-mapping.
 * @public
 */
export * from './app-effects.js';

/* ============================================================================
 * 🔐 ТИПЫ — AUTH DOMAIN TYPES
 * ============================================================================
 */

/**
 * Типы для auth domain.
 * Включает типы и DTO для аутентификации и авторизации.
 * @public
 */
export * from './auth.js';

/* ============================================================================
 * 🤖 ТИПЫ — BOTS DOMAIN TYPES
 * ============================================================================
 */

/**
 * Типы для bots domain.
 * Включает типы и DTO для ботов.
 * @public
 */
export * from './bots.js';

/* ============================================================================
 * 💬 ТИПЫ — CONVERSATIONS DOMAIN TYPES
 * ============================================================================
 */

/**
 * Типы для conversations domain.
 * Включает типы и DTO для разговоров.
 * @public
 */
export * from './conversations.js';

/* ============================================================================
 * 📊 ТИПЫ — TELEMETRY DOMAIN TYPES
 * ============================================================================
 */

/**
 * Типы для telemetry domain.
 * Включает фундаментальные типы телеметрии (TelemetryEvent, TelemetryConfig, TelemetrySink и т.д.).
 * Используются в core-модулях и публичном API.
 * @public
 */
export * from './telemetry.js';
