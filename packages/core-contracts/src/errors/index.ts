/**
 * @file @livai/core-contracts/errors — HTTP Error Types & Codes
 * Публичный API пакета errors.
 * Экспортирует все публичные компоненты, типы и константы для типов ошибок HTTP API.
 */

/* ============================================================================
 * 🚨 ТИПЫ — HTTP ERROR TYPES
 * ============================================================================
 */

/**
 * Типы для HTTP ошибок.
 * Включает ErrorResponse (единый контракт ошибки), ErrorCode (тип кодов ошибок).
 * Стандартизированные коды ошибок для единообразной обработки во всех сервисах.
 * @public
 */
export * from './http.js';
