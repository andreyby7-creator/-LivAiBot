# 🚀 Security Pipeline v2 Rollout Plan

**Версия:** 1.0\
**Статус:** Operational Readiness\
**Цель:** Безопасный rollout v2 pipeline в production

---

## 📋 Этапы Rollout

### Этап 0: Dry-Run (Shadow Mode 100%)

**Длительность:** 24-72 часа\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 100,
  activeV2TrafficPercentage: 0,
}
```

**Цель:** Накопление данных без влияния на решения

**Метрики для сбора:**

- `v2WeakerPercentage` — накопление статистики
- `v2StricterPercentage` — накопление статистики
- `providerTimeoutRate` — базовая линия
- `failClosedRate` — базовая линия
- `circuit breaker open %` — базовая линия

**Критерии перехода к следующему этапу:**

- ✅ Собрано минимум 10,000 сравнений
- ✅ Все метрики стабильны в течение 24 часов
- ✅ Нет аномалий в данных

---

### Этап 1: Canary 1%

**Длительность:** 24 часа\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 99,
  activeV2TrafficPercentage: 1,
}
```

**Мониторинг:**

- Проверка метрик каждый час
- Готовность к немедленному откату

**Go/No-Go критерии (для перехода к 5%):**

- ✅ `v2WeakerPercentage < 0.3%`
- ✅ `providerTimeoutRate < 1%`
- ✅ `failClosedRate < baseline + 20%`
- ✅ `circuit breaker open % < 5%`
- ✅ Нет критических инцидентов

**Действия при проблемах:**

- Немедленный откат: `FORCE_RISK_V1=1`
- Анализ причин
- Исправление перед повторным запуском

---

### Этап 2: Canary 5%

**Длительность:** 24 часа\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 95,
  activeV2TrafficPercentage: 5,
}
```

**Мониторинг:**

- Проверка метрик каждые 2 часа
- Мониторинг жалоб пользователей

**Go/No-Go критерии (для перехода к 20%):**

- ✅ Все метрики в норме в течение 24 часов
- ✅ `v2WeakerPercentage` стабильно < 0.3%
- ✅ Нет роста `failClosedRate`
- ✅ Нет жалоб пользователей

---

### Этап 3: Canary 20%

**Длительность:** 48 часов\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 80,
  activeV2TrafficPercentage: 20,
}
```

**Мониторинг:**

- Проверка метрик каждые 4 часа
- Расширенный мониторинг

**Go/No-Go критерии (для перехода к 50%):**

- ✅ Все метрики в норме в течение 48 часов
- ✅ Стабильность всех показателей
- ✅ Нет инцидентов

---

### Этап 4: Canary 50%

**Длительность:** 48 часов\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 50,
  activeV2TrafficPercentage: 50,
}
```

**Мониторинг:**

- Проверка метрик каждые 6 часов
- Финальная проверка перед полным rollout

**Go/No-Go критерии (для перехода к 100%):**

- ✅ Все метрики в норме в течение 48 часов
- ✅ Полная уверенность в стабильности
- ✅ Нет инцидентов

---

### Этап 5: Full Rollout (100%)

**Длительность:** Постоянный мониторинг\
**Конфигурация:**

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 0,
  activeV2TrafficPercentage: 100,
}
```

**Мониторинг:**

- Проверка метрик каждые 12 часов
- Готовность к откату при необходимости

**Критерии успеха:**

- ✅ Все метрики стабильны
- ✅ Нет деградации безопасности
- ✅ Нет проблем с производительностью

---

## 📊 Go/No-Go Критерии (Формальные правила)

### Обязательные условия для включения active_v2

**ВСЕ условия должны быть выполнены одновременно:**

1. **v2WeakerPercentage < 0.3%**
   - Измерение: за последние 24 часа
   - Критичность: 🔴 КРИТИЧНО
   - Действие при нарушении: НЕМЕДЛЕННЫЙ ОТКАТ

2. **providerTimeoutRate < 1%**
   - Измерение: за последние 24 часа
   - Критичность: 🔴 КРИТИЧНО
   - Действие при нарушении: ОТКЛЮЧИТЬ REMOTE PROVIDER

3. **failClosedRate < baseline + 20%**
   - Измерение: за последние 24 часа
   - Критичность: 🟡 ВАЖНО
   - Действие при нарушении: ПРОВЕРИТЬ ПРОВАЙДЕР

4. **circuit breaker open % < 5%**
   - Измерение: за последние 24 часа
   - Критичность: 🟡 ВАЖНО
   - Действие при нарушении: ОТКЛЮЧИТЬ REMOTE PROVIDER

5. **totalComparisons >= 10,000**
   - Измерение: минимум данных для принятия решения
   - Критичность: 🟡 ВАЖНО
   - Действие при нарушении: ПОДОЖДАТЬ НАКОПЛЕНИЯ ДАННЫХ

6. **Нет критических инцидентов за последние 48 часов**
   - Измерение: отсутствие инцидентов
   - Критичность: 🟡 ВАЖНО
   - Действие при нарушении: ОТЛОЖИТЬ ROLLOUT

---

## 🚨 Процедуры отката

### Немедленный откат (0-30 секунд)

```bash
# Установить environment variable
export FORCE_RISK_V1=1

# Перезапустить сервис
# (зависит от вашего deployment)
```

**Когда использовать:**

- `v2WeakerPercentage > 0.3%`
- Массовый fail-closed
- Provider outage
- Критические инциденты

---

### Частичный откат (1-5 минут)

```bash
# Отключить только remote provider
export DISABLE_REMOTE_PROVIDER=1

# ИЛИ снизить процент canary
# Изменить конфигурацию:
rolloutConfig: {
  activeV2TrafficPercentage: 0, // Вернуться на предыдущий этап
}
```

**Когда использовать:**

- Проблемы только с remote provider
- Нужно временно снизить нагрузку

---

## 📈 Метрики для мониторинга

### Ключевые метрики (проверять на каждом этапе)

1. **v2WeakerPercentage** — САМАЯ ВАЖНАЯ
   - Порог: < 0.3%
   - Частота проверки: каждый час

2. **providerTimeoutRate**
   - Порог: < 1%
   - Частота проверки: каждый час

3. **failClosedRate**
   - Порог: < baseline + 20%
   - Частота проверки: каждые 2 часа

4. **circuit breaker open %**
   - Порог: < 5%
   - Частота проверки: каждые 2 часа

5. **totalComparisons**
   - Минимум: 10,000
   - Частота проверки: каждые 6 часов

---

## ⏸️ Паузы между этапами

**Минимум 24 часа паузы** между каждым этапом для:

- Анализа метрик
- Проверки стабильности
- Подготовки к следующему этапу

**Исключение:** Если все метрики идеальны и нет инцидентов, можно сократить до 12 часов (только с одобрения Security Lead).

---

## 📝 Чеклист перед каждым этапом

- [ ] Все метрики предыдущего этапа в норме
- [ ] Нет активных инцидентов
- [ ] On-call инженер доступен
- [ ] Runbook обновлен и доступен
- [ ] Runtime overrides протестированы
- [ ] Мониторинг настроен
- [ ] Готовность к немедленному откату

---

## 🔄 Процедура после инцидента

1. **Немедленный откат** (0-30 секунд)
2. **Анализ причин** (1-4 часа)
3. **Исправление** (зависит от проблемы)
4. **Тестирование** (на replay dataset)
5. **Повторный запуск** (с предыдущего этапа)

**Важно:** После инцидента нельзя сразу переходить к следующему этапу — нужно повторить текущий этап.

---

**Последнее обновление:** 2026-XX-XX\
**Ответственный:** Security Team
