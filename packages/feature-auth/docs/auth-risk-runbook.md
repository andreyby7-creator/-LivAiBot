# 🛡 Security Pipeline Runbook (Auth)

**Статус:** Production\
**Назначение:** On-call и Security\
**Цель:** Быстрое реагирование, минимизация blast radius

> 📘 **Generic документация:** Для общих принципов pipeline см. [`@livai/core/docs/pipeline-runbook.md`](../../core/docs/pipeline-runbook.md)

---

## 🚨 30-СЕКУНДНЫЕ ДЕЙСТВИЯ

### Полностью отключить v2

```bash
export FORCE_RISK_V1=1
```

Перезапустить сервис.

**Эффект:**

- 100% трафика → v1
- v2 полностью выключен

---

### Отключить только remote provider

```bash
export DISABLE_REMOTE_PROVIDER=1
```

Перезапустить сервис.

**Эффект:**

- Работают только local rules
- Circuit breaker больше не влияет

---

### Emergency fail-open (ТОЛЬКО при массовой блокировке)

```bash
export FAIL_OPEN_MODE=1
```

Перезапустить сервис.

**⚠️ Снижает безопасность. Использовать кратковременно.**

---

## 📊 КЛЮЧЕВЫЕ МЕТРИКИ

**Проверяются на каждом инциденте.**

### 1️⃣ Disagreement Metrics

- **`v2WeakerPercentage`**
  - 🟢 Норма: < 0.3%
  - 🔴 Критично: > 0.3% → Откат

- **`v2StricterPercentage`**
  - 🟢 Норма: любое значение
  - ℹ️ Нормально (это хорошо)

- **`exactMatchPercentage`**
  - 🟢 Норма: > 95%
  - 🟡 Проверка: < 90%

- **`totalComparisons`**
  - 🟢 Норма: ≥ 1000
  - 🟡 Недостаточно данных: < 1000

**Главное правило:** Если `v2WeakerPercentage > 0.3%` → НЕМЕДЛЕННЫЙ ОТКАТ.

---

### 2️⃣ Provider / Resilience Metrics

- **`providerTimeoutRate`**
  - 🟢 Норма: < 1%
  - 🔴 Критично: > 5% → Отключить provider

- **`failClosedRate`**
  - 🟢 Норма: < baseline +20%
  - 🔴 Критично: > baseline +50% → Проверить provider

- **`circuit breaker open %`**
  - 🟢 Норма: < 5%
  - 🔴 Критично: > 15% → Отключить provider

---

## 🚨 ИНЦИДЕНТ-СЦЕНАРИИ

### Сценарий A: Массовый fail-closed

**Симптомы:**

- Пользователи не могут войти
- `failClosedRate` резко вырос
- Логи: `fail-closed triggered`

**Действия:**

```bash
export FORCE_RISK_V1=1
export DISABLE_REMOTE_PROVIDER=1
```

Перезапустить сервис.

**Далее:**

1. Проверить `providerTimeoutRate`
2. Проверить circuit breaker
3. Проверить доступность provider

**Критерий стабилизации:** `failClosedRate` вернулся к baseline.

---

### Сценарий B: Provider outage

**Симптомы:**

- `providerTimeoutRate ≈ 100%`
- Circuit breaker открыт

**Действие:**

```bash
export DISABLE_REMOTE_PROVIDER=1
```

Перезапустить сервис.

Работаем только на local rules.

**Включать обратно** — только через canary.

---

### Сценарий C: v2 слабее v1

**Симптомы:**

- `v2WeakerPercentage > 0.3%`
- Рост weaker count

**Действие:**

```bash
export FORCE_RISK_V1=1
```

Перезапустить сервис.

**Далее:**

1. Проверить изменения в risk rules
2. Проверить изменения в provider
3. Проверить aggregation policy

**Возвращать v2** только после 24ч стабильности.

---

### Сценарий D: Массовые false positives

**Симптомы:**

- Рост жалоб
- `v2StricterPercentage` аномально высокий
- Legit users блокируются

**Действия:**

```bash
export FORCE_RISK_V1=1
```

При необходимости:

```bash
export FAIL_OPEN_MODE=1
```

После стабилизации — replay dataset → canary rollout.

---

## 🔧 РУЧНОЕ УПРАВЛЕНИЕ

### Отключить v2 для конкретного tenant / bucket

**Через rolloutConfig:**

```typescript
rolloutConfig: {
  v2EnabledTenants: [],
  v2EnabledBuckets: [],
}
```

---

### Shadow Mode (Dry Run)

```typescript
rolloutConfig: {
  shadowV2TrafficPercentage: 100,
  activeV2TrafficPercentage: 0,
}
```

**Используется:**

- перед rollout
- после инцидента
- для проверки новой версии

---

## 📈 GO / NO-GO ПРАВИЛА

**Все условия должны выполняться одновременно:**

- `v2WeakerPercentage < 0.3%`
- `providerTimeoutRate < 1%`
- `failClosedRate < baseline + 20%`
- `circuit breaker open % < 5%`
- `totalComparisons ≥ 10000`
- Нет инцидентов за 48ч

**Если хотя бы одно нарушено → rollout запрещён.**

---

## 🔄 CANARY PLAN (КРАТКО)

1. **Shadow 100%** — 24–72ч
2. **1%** — 24ч
3. **5%** — 24ч
4. **20%** — 48ч
5. **50%** — 48ч
6. **100%** — мониторинг

**Между этапами** — минимум 24ч стабильности.

---

## 🛠 RUNTIME SAFETY SWITCHES

- **`FORCE_RISK_V1=1`**
  - Назначение: Полный откат на v1

- **`DISABLE_REMOTE_PROVIDER=1`**
  - Назначение: Отключить remote

- **`FAIL_OPEN_MODE=1`**
  - Назначение: Emergency allow

**Все требуют перезапуска сервиса.**

---

## 🔐 Auth-Специфика

Auth использует generic pipeline из `@livai/core`.

**Особенности:**

- Risk assessment применяется на login / auth flows
- Shadow и active режим управляются через `rolloutConfig`
- Все emergency-переключатели доступны через env

**Интеграция:**

```typescript
import { createPipelineConfig } from '@livai/core/pipeline';

const config = createPipelineConfig({
  rolloutConfig: {
    shadowV2TrafficPercentage: 100,
    activeV2TrafficPercentage: 0,
    v2EnabledTenants: [],
    v2EnabledBuckets: [],
  },
});
```

---

## 🧠 После любого инцидента

1. Откат
2. RCA
3. Replay dataset
4. Shadow
5. Canary
6. Только потом масштабирование

**Никогда не переходить сразу на следующий этап.**

---

## 🧾 Принцип

**Безопасность > Доступность > Новая версия.**

Если есть сомнение — откатываемся.
