# ESLint Canary Mode - Исправление ошибок

## Проблема

После билда результаты линтинга различались: до билда — 0 ошибок, после билда — 31 ошибка. Билд создает `.d.ts` файлы в `dist/`, которые влияют на type resolution в TypeScript Language Service. ESLint с `projectService: true` использует TypeScript Language Service для type-aware правил, поэтому результаты линтинга различаются до и после билда.

## Решение

Использование type guards (`isErrorSeverity`, `isErrorCategory`, `isErrorOrigin`) и проверок типов (`typeof`) вместо type assertions. Это обеспечивает корректную типизацию независимо от наличия билда.

### Изменения в коде:

1. **ErrorMetadata.ts**: Использование type guards и проверок типов при извлечении значений из `Partial<ErrorMetadata>`
2. **ErrorUtils.ts**: Использование type guards для безопасного возврата значений
3. **ErrorSerialization.ts**: Использование type guards вместо type assertions

### Конфигурация ESLint:

Добавлен глобальный `ignores` на верхнем уровне конфигурации для исключения `dist/**/*.d.ts` файлов.

## Результат

✅ **0 ошибок до и после билда**  
✅ **0 warnings**  
✅ **Стабильные результаты линтинга независимо от наличия билда**

---

## Мини-гайд: Предотвращение аналогичных проблем

### ✅ Чеклист при работе с type-aware правилами ESLint

1. **Всегда проверяйте линтинг БЕЗ кэша после билда:**
   ```bash
   rm -rf dist && pnpm run build && pnpm run lint:canary --no-cache
   ```

2. **Используйте type guards вместо type assertions:**
   - ✅ `isErrorSeverity(value) ? value : fallback`
   - ❌ `value as ErrorSeverity`

3. **Используйте проверки типов для неизвестных значений:**
   - ✅ `typeof value === "string" && value.length > 0`
   - ❌ `value as string`

4. **Исключайте `dist/` из линтинга:**
   - Добавьте `ignores: ['**/dist/**']` на верхнем уровне ESLint конфигурации

5. **Проверяйте результаты до и после билда:**
   - Если результаты различаются → проблема с type resolution
   - Решение: используйте type guards и проверки типов

### 🔍 Диагностика

Если видите разные результаты линтинга до/после билда:
1. Проверьте, используются ли type assertions вместо type guards
2. Убедитесь, что `dist/` исключен из линтинга
3. Проверьте, что TypeScript Language Service видит типы корректно
