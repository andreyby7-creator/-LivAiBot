# 🎯 Стратегия Immutability для LivAiBot (TypeScript + Pure FP + Effect)

## 📋 Проблема

ESLint правило `functional/prefer-immutable-types` требует уровень "Immutable", но `ReadonlyDeep` из `type-fest` не признается как достаточный уровень. Правило настроено на максимальную строгость, что конфликтует с практическими нуждами Effect-экосистемы.

---

## ✅ **РЕШЕНИЕ #1: Настроить ESLint правило правильно (РЕКОМЕНДУЕТСЯ)**

### Суть решения

**Использовать `type-fest` + правильная настройка ESLint правила `functional/prefer-immutable-types` с `enforcement: "ReadonlyDeep"`**

### Почему это правильное решение:

1. ✅ **`ReadonlyDeep` из `type-fest` - industry standard** для deep immutability в TypeScript
2. ✅ **Используется во всех современных FP проектах** (Effect, fp-ts экосистема)
3. ✅ **Полностью покрывает нужды FP**: делает все свойства глубоко readonly
4. ✅ **Совместим с Effect**: Effect использует ReadonlyDeep паттерн для своих структур
5. ✅ **Нет костылей**: нативная поддержка в ESLint правиле
6. ✅ **Работает везде**: backend, frontend, domain, infrastructure

### Реализация:

```typescript
// config/eslint/modes/canary.config.mjs или constants.js
'functional/prefer-immutable-types': [
  'error',
  {
    enforcement: 'ReadonlyDeep', // ✅ Признаем ReadonlyDeep как достаточный уровень
    ignoreInferredTypes: false,
    ignoreClasses: true, // Effect использует классы для типа безопасности
    ignoreInterfaces: false,
  }
]
```

### Что использовать в коде:

```typescript
import type { ReadonlyDeep } from "type-fest"

// ✅ Правильно для всех immutable структур
export type BaseError = ReadonlyDeep<{...}>
export type ErrorMetadata = ReadonlyDeep<{...}>

// ✅ Правильно для функций
export const getCauseChain = (
  error: ReadonlyDeep<BaseError>,
  maxDepth = 10
): ReadonlyArray<unknown> => { ... }
```

### Плюсы:
- ✅ Нет необходимости в новых зависимостях
- ✅ Стандартный подход в TypeScript FP сообществе
- ✅ ESLint правильно проверяет использование
- ✅ Type-safe на 100%
- ✅ Работает с Effect, fp-ts, monocle-ts

### Минусы:
- ❌ Требует правильной настройки ESLint (1 раз)

---

## 🔄 **РЕШЕНИЕ #2: Использовать Effect встроенные структуры (для данных)**

### Суть решения

**Использовать Effect.Chunk, Effect.HashSet, Effect.HashMap для коллекций вместо обычных массивов/объектов**

### Когда использовать:

- ✅ **Коллекции данных**: списки, множества, словари
- ✅ **Большие структуры данных**: где нужна производительность
- ✅ **Операции с данными**: фильтрация, трансформации

### Пример:

```typescript
import { Chunk } from "effect"

// ✅ Вместо ReadonlyArray для больших коллекций
export const filterErrorsBySeverity = <T extends ReadonlyDeep<BaseError>>(
  errors: Chunk<T>, // ✅ Effect.Chunk - immutable по дизайну
  severity: ErrorSeverity
): Chunk<T> =>
  Chunk.filter(errors, (error) => getErrorSeverity(error) === severity)
```

### Плюсы:
- ✅ Встроенная immutability (структурное разделение)
- ✅ Высокая производительность для больших коллекций
- ✅ Идеальная интеграция с Effect
- ✅ Оптимизированные операции

### Минусы:
- ❌ Дополнительная зависимость на Effect runtime
- ❌ Не подходит для всех случаев (например, error metadata)
- ❌ Больше boilerplate для простых случаев

### Когда НЕ использовать:
- ❌ Простые структуры (ErrorMetadata, BaseError)
- ❌ Где важна совместимость с обычными JS объектами
- ❌ JSON сериализация (нужно конвертировать)

---

## 🔧 **РЕШЕНИЕ #3: Отключить правило для конкретных зон (компромисс)**

### Суть решения

**Использовать ESLint disable comments для файлов, где Effect типы требуют гибкости**

### Когда использовать:

- ✅ **Boundary слои**: где нужно конвертировать между Effect и обычными типами
- ✅ **Adapter слои**: нормализация данных
- ✅ **Legacy интеграция**: когда нельзя изменить структуру

### Пример:

```typescript
// core-contracts/src/errors/base/ErrorUtils.ts
/* eslint-disable functional/prefer-immutable-types */
// ⚠️ Effect.Chunk и Generator требуют гибкости типов
export const getCauseChain = (
  error: ReadonlyDeep<BaseError>,
  maxDepth = 10
): ReadonlyArray<unknown> => { ... }
```

### Плюсы:
- ✅ Максимальная гибкость
- ✅ Можно использовать в специфичных случаях

### Минусы:
- ❌ Нарушает консистентность
- ❌ Требует discipline от разработчиков
- ❌ Не масштабируется

---

## 🎯 **ИТОГОВАЯ РЕКОМЕНДАЦИЯ для LivAiBot**

### **Использовать РЕШЕНИЕ #1** (правильная настройка ESLint + type-fest)

### Почему именно это:

1. ✅ **Стандарт индустрии**: `ReadonlyDeep` из `type-fest` - это то, что используют все серьезные FP проекты
2. ✅ **Покрывает все нужды**: backend, frontend, domain, infrastructure
3. ✅ **Нет костылей**: нативная поддержка в ESLint
4. ✅ **Effect-friendly**: Effect использует тот же подход
5. ✅ **Масштабируется**: работает для всего стека
6. ✅ **Type-safe**: полная поддержка TypeScript

### Структура использования:

```
📦 Core Contracts (foundation)
├── ✅ ReadonlyDeep для всех domain types (BaseError, ErrorMetadata, etc.)
├── ✅ ReadonlyDeep для всех DTO/структур
└── ✅ ReadonlyArray для простых коллекций

📦 Application Layer
├── ✅ ReadonlyDeep для всех use case inputs/outputs
├── ✅ Effect.Chunk для больших коллекций (опционально)
└── ✅ ReadonlyDeep для domain events

📦 Infrastructure
├── ✅ ReadonlyDeep для всех адаптеров
└── ✅ ReadonlyDeep для всех контрактов

📦 Frontend
├── ✅ ReadonlyDeep для всех state структур
├── ✅ ReadonlyDeep для всех props/context
└── ✅ ReadonlyArray для списков в UI
```

### Дополнительные рекомендации:

1. **Для больших коллекций** (1000+ элементов):
   - Рассмотреть `Effect.Chunk` вместо `ReadonlyArray`
   - Использовать `Effect.HashMap` для lookup структур

2. **Для runtime проверок**:
   - Использовать `Object.freeze()` в factory функциях (уже делается в `createError`)
   - Использовать `deepFreeze` для вложенных структур

3. **Для паттерн матчинга**:
   - Использовать `effect/Match` из основного пакета `effect` (merged в effect 3.x)
   - Использовать `ts-pattern` для discriminated unions

---

## 📚 **Зависимости (уже есть в проекте)**

```json
{
  "type-fest": "^5.3.1", // ✅ Уже есть - используем ReadonlyDeep
  "effect": "^3.19.12"   // ✅ Уже есть - для Chunk/HashMap, Match, Schema при необходимости
}
```

**Никаких дополнительных зависимостей не требуется!** ✅

---

## 🔧 **Что нужно исправить**

1. **Настроить ESLint правило** в `config/eslint/modes/canary.config.mjs`:
   ```javascript
   'functional/prefer-immutable-types': [
     'error',
     { enforcement: 'ReadonlyDeep' }
   ]
   ```

2. **Проверить все использования** - убедиться что везде используется `ReadonlyDeep<...>`

3. **Обновить документацию** - зафиксировать стандарт в архитектурной документации

---

## ✅ **Результат**

- ✅ Чистый FP подход без костылей
- ✅ Работает для всего стека (backend + frontend)
- ✅ Совместим с Effect экосистемой
- ✅ Type-safe на 100%
- ✅ ESLint проверяет правильно
- ✅ Масштабируется для любого размера проекта
- ✅ Соответствует best practices TypeScript FP сообщества
